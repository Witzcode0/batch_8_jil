#include <stdio.h>

int main() {
    int num = 10;
    
    // num = num + 20;
    // num += 20;
    // num -= 20;
    // num *= 20;
    // num /= 20;
    // num %= 20;
    
    printf("%d", num);

    return 0;
}